/**
 * @Author Jay Lee
 * @Version 0.1
 * This is a basic gantt chart.
 * */
(function() { 'use strict';
    /**
     * @Param {string} selector
     * @Return {object} SimChart
     * */
    function SimGantt(selector, options) {
        if(!(this instanceof SimGantt)) {
            return new SimGantt(selector, options);
        }
    }
})();
